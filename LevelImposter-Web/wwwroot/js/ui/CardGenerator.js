export class CardGenerator {
    generate() {
    }
    genCard() {
        let card = document.createElement("div");
        card.classList.add("collider-prop");
        return;
    }
}
//# sourceMappingURL=CardGenerator.js.map